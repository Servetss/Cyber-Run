using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Move { Left, Stay, Right, Up, Down};
public enum Events { Empty, Tramplin, Wall, FloorDeath }
public enum Collision { Begin, End}
